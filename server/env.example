# ============================================
# MERN MEMORIES - ENVIRONMENT VARIABLES
# ============================================
# Copy this file to .env and update with your actual values
# Command: cp env.example .env
# Never commit .env to version control!

# ============================================
# DATABASE CONFIGURATION
# ============================================
# PostgreSQL Connection String
# For local PostgreSQL:
DATABASE_URL=postgresql://username:password@localhost:5432/memories?schema=public

# For PostgreSQL on cloud (e.g., Supabase, Railway, Render):
# DATABASE_URL=postgresql://username:password@host:5432/database?schema=public&sslmode=require

# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=SCHEMA

# ============================================
# SERVER CONFIGURATION
# ============================================
# Server Port (optional - defaults to 5000 if not set)
PORT=5000

# ============================================
# JWT AUTHENTICATION
# ============================================
# JWT Secret Key - MUST be a strong random string (minimum 32 characters)
# Generate one using: openssl rand -base64 32
# Or use: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
JWT_SECRET=your_super_secret_jwt_key_min_32_characters_change_this_in_production

# ============================================
# GOOGLE OAUTH (Required for Google Sign-In)
# ============================================
# Google OAuth 2.0 Client ID for server-side token validation
# This should be the SAME Client ID used in client/.env
# Get this from: https://console.cloud.google.com/
# 1. Create a new project or select existing
# 2. Enable Google+ API (or Google Identity Services)
# 3. Go to "Credentials" > "Create Credentials" > "OAuth client ID"
# 4. Choose "Web application"
# 5. Copy the Client ID here
GOOGLE_CLIENT_ID=your_google_client_id_here

# Note: GOOGLE_CLIENT_SECRET is NOT needed for ID token verification
# The server only needs the Client ID to verify tokens

# ============================================
# PRODUCTION NOTES
# ============================================
# For production, make sure to:
# 1. Use a strong, unique JWT_SECRET (never use the default!)
# 2. Use MongoDB Atlas or a secure database connection
# 3. Update GOOGLE_CLIENT_ID with production URLs
# 4. Set NODE_ENV=production
# 5. Use HTTPS in production

# ============================================
# QUICK START
# ============================================
# 1. Copy this file: cp env.example .env
# 2. Update MONGODB_URI with your database connection
# 3. Generate a JWT_SECRET (see instructions above)
# 4. (Optional) Add GOOGLE_CLIENT_ID for Google sign-in
# 5. Save the file and restart your server
